"""
Model training and evaluation routines.

This module defines functions that:
- fit supervised learning models
- generate predictions
- return standardized outputs (metrics, fitted models)

Each function should encapsulate one modeling approach
(e.g., logistic regression, tree-based models, etc.).
"""